import { DetailPanneauxPipe } from './detail-panneaux.pipe';

describe('DetailPanneauxPipe', () => {
  it('create an instance', () => {
    const pipe = new DetailPanneauxPipe();
    expect(pipe).toBeTruthy();
  });
});
